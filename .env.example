# AI/OCR API Configuration (Optional)
# If not configured, the app will use demo mode
OPENAI_API_KEY=your_openai_api_key_here
# Or other vision/LLM API keys as needed

# Server Configuration
HOST=127.0.0.1
PORT=8000
